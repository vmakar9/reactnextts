import {Metadata} from "next";

export const metadata: Metadata = {
    title: 'Posts'

}
const Page = () => {
    return (
        <div>
            PostPage
        </div>
    );
};

export default Page;